openapi: "3.1.0"
info:
  title: allOf Schemas
  version: "1.0.0"
paths: {}
components:
  schemas:
    Base:
      type: object
      required:
        - id
      properties:
        id:
          type: integer
          format: int64
    Extended:
      allOf:
        - $ref: "#/components/schemas/Base"
        - type: object
          required:
            - name
          properties:
            name:
              type: string